// TODO: INTEGRATION CONTRACT
// Replace with actual company memory schema. The exact JSON structure is currently unknown.

export interface CompanyMemory {
  // Unknown / flexible structure
  [key: string]: any;
}
