// Team 2 (2.1 Signal Discovery) — Owner: Veeranjaneyulu Kopparapu
// Normalizes raw job posting text/URL into a hiring_signals row.
// Input: raw signal from n8n ingestion webhook
// Output: hiring_signals row (company, role, hiring_type guess)
export async function runDiscoveryAgent(rawSignal: unknown) {
  // TODO: implement
}
